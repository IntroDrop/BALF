app_name = u'图片标注小工具'
max_label_num = 10
profile = 'profile.json'

help_text = """1. 快捷键: 左方向键(上一张), 右方向键(下一张,空格), 标签1(数字键1), 标签2(数字键2), 标签3(数字键3)...
2. 流程: 选择打开文件路径->选择改变图像保存路径->显示图片后可以开始打标签
3. 退出时会保存配置, 当再次打开后会恢复此前的状态"""